# Use legacy peer deps to resolve Hardhat conflicts
# Hardhat dependencies are only needed for contract development, not for Next.js build
legacy-peer-deps=true

# Optional: speed up installs
prefer-offline=true
